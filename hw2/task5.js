// Напишите функцию calculateDiff, которая принимает два числа и возвращает их разность.

const calculateDiff = (a, b) => {
    return a - b;
}

console.log(calculateDiff(10, 5));
console.log(calculateDiff(20, 10));
console.log(calculateDiff(30, 15));